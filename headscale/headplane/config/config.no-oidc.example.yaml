server:
    host: "0.0.0.0"
    port: 3000
    cookie_secret: "01234567890123456789012345678901"
    cookie_secure: false

headscale:
    url: "https://headscale.jinli.io"
    config_path: "/etc/headscale/config.yaml"
    config_strict: true

integration:
    docker:
        enabled: true
        container_name: "headscale"
        socket: "unix:///var/run/docker.sock"