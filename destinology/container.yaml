schema: 1
service: destinology
projectName: mdbook
compose:
  root: private/destinology/mdbook
  base:
    - docker-compose.yml
  hostOverlay: local/{host}/destinology/compose.{host}.yaml
environment: {}
files: {}
networks:
  required:
    - traefik-net
data:
  declarations: []
validation:
  healthChecks:
    - name: nginx-config
      command: ["nginx", "-t"]
placement:
  allowedHosts: [t460s]
  mode: singleton
autostart: false
compatibilityPaths: []
