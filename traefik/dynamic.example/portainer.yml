http:
  routers:
    portainer-router:
      entryPoints:
        - websecure
      tls: {}
      rule: "Host(`portainer.example.com`)"
      service: portainer-service
      middlewares:
        - portainer-headers

  services:
    portainer-service:
      loadBalancer:
        servers:
          - url: "http://portainer:9000"

  middlewares:
    portainer-headers:
      headers:
        customRequestHeaders:
          Origin: ""