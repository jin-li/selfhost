http:
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.example.com`)"
      service: api@internal
      entryPoints:
        - websecure
      tls: {}
      middlewares:
        - auth

  middlewares:
    auth:
      basicAuth:
        users:
          - username:hash_of_your_password
